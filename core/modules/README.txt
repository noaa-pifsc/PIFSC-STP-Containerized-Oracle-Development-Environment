This folder contains the different projects that extend the CODE functionality, the template for a given project can be found in the core/template/project folder

The .active_project file defines the active project that is being run in the CODE framework