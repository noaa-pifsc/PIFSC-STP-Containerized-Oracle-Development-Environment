services:
  code-db-ords-deploy:
    secrets:
      - [secret_username]
      - [secret_password]
secrets:
  [secret_username]:
    name: ${COMPOSE_PROJECT_NAME}_[secret_username]
    external: true
  [secret_password]:
    name: ${COMPOSE_PROJECT_NAME}_[secret_password]
    external: true